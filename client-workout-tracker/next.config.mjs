/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'raw.githubusercontent.com'
        ]
    }
};

export default nextConfig;
